name: benchmark
description: A sample command-line application.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.12.2

# Add regular dependencies here.
dependencies:
  daxle:
    path: ../daxle/
  fpdart: ^1.2.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
